import styled from "styled-components";

const TradesContainer = styled.ul`
    overflow-y: scroll;
    width: 100%;

    display: flex;
    flex-direction: column;
    gap: 10px;

    padding-bottom: 10px;
`;

export default TradesContainer;
