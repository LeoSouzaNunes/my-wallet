import styled from "styled-components";

const MainDisplay = styled.main`
    width: 100%;
    min-height: 450px;

    padding: 20px 10px;

    font-style: normal;
    font-weight: normal;
    font-size: 1.25rem;
    text-align: center;

    color: #868686;

    background: #ffffff;
    border-radius: 5px;
`;

export default MainDisplay;
